from django.contrib import admin
# from .models import Student, Course, CourseSchedule, StudentRegistration
#
# # Register models
# admin_app.site.register(Student)
# admin_app.site.register(Course)
# admin_app.site.register(CourseSchedule)
# admin_app.site.register(StudentRegistration)

